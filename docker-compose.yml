services:
  web:
    build:
      context: .
    container_name: mk-site
    restart: unless-stopped
    ports:
      - "80:3000"
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
      PORT: 3000
      HOST: 0.0.0.0
